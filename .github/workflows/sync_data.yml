name: Sync Data (Dev → Prod)

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Choose mode: dry-run or full-sync"
        required: true
        default: "dry-run"
        type: choice
        options:
          - dry-run
          - full-sync

jobs:
  sync-data:
    runs-on: ubuntu-latest
    name: Sync Data (Dev → Prod)

    env:
      AWS_REGION: ap-southeast-1
      AWS_BUCKET_DEV_DUMP: jiujitsuteria-mediia/dev-dumps
      AWS_BUCKET_BACKUP: jiujitsuteria-mediia/backups
      DEV_DB_FILE: db.sqlite3

    steps:
      # -------------------------
      # 1️⃣ Checkout code
      # -------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -------------------------
      # 2️⃣ Set mode variable
      # -------------------------
      - name: Set mode variable
        run: |
          MODE="${{ github.event.inputs.mode }}"
          echo "MODE=$MODE" >> $GITHUB_ENV
          echo "Workflow mode set to: $MODE"

      # -------------------------
      # 3️⃣ Setup Python
      # -------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      # -------------------------
      # 4️⃣ Install dependencies (pinned)
      # -------------------------
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install boto3==1.38.37 "botocore>=1.38.37,<1.39.0" "s3transfer>=0.13.0,<0.14.0"
          pip install -r requirements.txt
          pip install awscli

      # -------------------------
      # 5️⃣ Configure AWS CLI
      # -------------------------
      - name: Configure AWS CLI
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # -------------------------
      # 6️⃣ Ensure Dev SQLite DB
      # -------------------------
      - name: Ensure Dev SQLite DB
        run: |
          echo "Checking if dev database exists locally..."
          if [ ! -f "${{ env.DEV_DB_FILE }}" ]; then
            echo "Attempting to download dev DB from S3..."
            if aws s3 ls "s3://${{ env.AWS_BUCKET_DEV_DUMP }}/${{ env.DEV_DB_FILE }}" > /dev/null 2>&1; then
              aws s3 cp "s3://${{ env.AWS_BUCKET_DEV_DUMP }}/${{ env.DEV_DB_FILE }}" .
              echo "✅ Dev database downloaded successfully."
            else
              echo "⚠️  WARNING: Dev DB not found in S3. Continuing with local context..."
            fi
          else
            echo "✅ Dev DB already present locally."
          fi

          if [ ! -f "${{ env.DEV_DB_FILE }}" ]; then
            echo "❌ ERROR: db.sqlite3 not found locally or in S3."
            exit 1
          fi

      # -------------------------
      # 7️⃣ Run sync_data.sh
      # -------------------------
      - name: Run sync_data.sh
        run: |
          chmod +x scripts/sync_data.sh
          if [ "$MODE" = "dry-run" ]; then
            echo "🔍 Running dry-run..."
            ./scripts/sync_data.sh --dry-run
          else
            echo "🚀 Running full data sync (Prod will be updated)..."
            ./scripts/sync_data.sh

      # -------------------------
      # 8️⃣ Completion log
      # -------------------------
      - name: Sync Complete
        if: always()
        run: |
          echo "✅ Sync data job finished successfully (mode: $MODE)"

