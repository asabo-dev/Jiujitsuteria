name: Sync Data (Dev → Prod)

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Choose mode: dry-run, full-sync-dry-run, or full-sync"
        required: true
        default: "dry-run"
        type: choice
        options:
          - dry-run
          - full-sync-dry-run
          - full-sync

env:
  AWS_REGION: ap-southeast-1
  AWS_BUCKET_DEV_DUMP: jiujitsuteria-mediia/dev-dumps
  AWS_BUCKET_BACKUP: jiujitsuteria-mediia/backups
  DEV_DB_FILE: db.sqlite3

jobs:
  sync-data:
    runs-on: ubuntu-latest
    name: Run sync data (mode: ${{ github.event.inputs.mode }})
    environment: ${{ github.event.inputs.mode == 'full-sync' || github.event.inputs.mode == 'full-sync-dry-run'  && 'production' || 'development' }}

    steps:
      # -------------------------
      # 1️⃣ Checkout repository
      # -------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # -------------------------
      # 2️⃣ Setup Python
      # -------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      # -------------------------
      # 3️⃣ Install dependencies
      # -------------------------
      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          pip install "boto3==1.38.37" "botocore>=1.38.37,<1.39.0" "s3transfer>=0.13.0,<0.14.0"
          pip install -r requirements.txt
          pip install awscli

      # -------------------------
      # 4️⃣ Configure AWS credentials
      # -------------------------
      - name: Configure AWS CLI
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # -------------------------
      # 5️⃣ Ensure Dev SQLite DB is available
      # -------------------------
      - name: Download Dev SQLite DB from S3
        run: |
          echo "Checking if dev database exists locally..."
          if [ ! -f "${{ env.DEV_DB_FILE }}" ]; then
            echo "Attempting to download dev DB from S3..."
            if aws s3 ls "s3://${{ env.AWS_BUCKET_DEV_DUMP }}/${{ env.DEV_DB_FILE }}" > /dev/null 2>&1; then
              aws s3 cp "s3://${{ env.AWS_BUCKET_DEV_DUMP }}/${{ env.DEV_DB_FILE }}" .
              echo "✅ Dev database downloaded successfully."
            else
              echo "❌ ERROR: Dev DB not found in S3. Upload db.sqlite3 or switch to dry-run."
              exit 1
            fi
          else
            echo "✅ Dev DB already present locally."

      # -------------------------
      # 6️⃣ Run data sync script
      # -------------------------
      - name: Run sync_data.sh
        run: |
          chmod +x scripts/sync_data.sh
          if [ "${{ github.event.inputs.mode }}" == "dry-run" ] || [ "${{ github.event.inputs.mode }}" == "full-sync-dry-run" ]; then
            echo "🔍 Running dry-run..."
            ./scripts/sync_data.sh --dry-run
          else
            echo "🚀 Running full data sync (Prod will be updated)..."
            ./scripts/sync_data.sh

      # -------------------------
      # 7️⃣ Completion log
      # -------------------------
      - name: Sync Complete
        if: always()
        run: |
          echo "✅ Sync data job finished successfully (mode: ${{ github.event.inputs.mode }})"

